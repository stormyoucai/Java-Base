1、public class 和 clss 声明的区别？
在整个Java文件中只允许有一个public class声明，但是允许有多个class声明
public class 文件名称必须与类名称一致，但是class文件名称可以不与类名称一致

2、文档注释：/** ... */ 的形式表示

3、Java标识符命名规则：
Java中的包、类、方法、变量、参数的名字，可由字母、数字、下划线、美元符号($)组成，
但标识符不能以数字开头

4、Java中哪些关键字是没有使用到的？
goto、const

5、true、false、null在Java中有特殊意义，不能用作标识符

6、变量：将内存中的某个内存块保留下来以供程序使用，里面的内容是可变的（可以改变内容的区域）
   常量：常量内容是不会改变的（比如数字1就是一个常量）
